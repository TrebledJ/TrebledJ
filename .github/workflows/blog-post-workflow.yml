name: Latest blog post workflow
on:
    workflow_dispatch:
    schedule:
        - cron: '0 * * * *'

jobs: 
    update-readme-with-blog: 
        name: Update this repo's README with latest blog posts
        runs-on: ubuntu-latest
        steps: 
            - uses: actions/checkout@v2
            - uses: gautamkrishnar/blog-post-workflow@master
              with: 
                max_post_count: "5"
                feed_list: "https://trebledj.github.io/feed.xml"
                template: "$newline - $randomEmoji(🔥,🚀,🌞,📈,😱,😮,🙃) <a href='$url' target='_blank'>$title</a> | 🏷️ **Tags**: $categories | 📅 $date"
                categories_template: "<a href='https://trebled.github.io/tags/$category' target='_blank'>$category</a>"
                date_format: "yyyy-mm-dd"
